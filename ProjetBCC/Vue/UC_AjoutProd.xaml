<UserControl x:Class="ProjetBCC.UC_AjoutProd"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjetBCC"
             mc:Ignorable="d" 
             d:DesignHeight="500" Width="546.5">
    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBlock Text="Ajouter un produit" FontSize="36" TextAlignment="Center"/>
            <TextBlock Text="Nom :"
                    TextWrapping="Wrap" Width="300"
                    TextAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16"/>
            <TextBox Name="nomProduit" Text="{Binding nomProduitProperty, Mode=OneWay}" Width="250" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            <TextBlock Text="Estimation :"
                     TextWrapping="Wrap" Width="300"
                    TextAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16"/>
            <TextBox Name="estimation" Text="{Binding estimationProperty, Mode=OneWay}" Width="250" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            <TextBlock Text="Description :"
                     TextWrapping="Wrap" Width="300"
                    TextAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16"/>
            <TextBox Name="description" Text="{Binding descriptionProperty, Mode=OneWay}" Width="250" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            <TextBlock Text="Artiste :"
                     TextWrapping="Wrap" Width="300"
                    TextAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16"/>
            <TextBox Name="artiste" Text="{Binding artisteProperty, Mode=OneWay}" Width="250" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            <TextBlock Text="Style :"
                     TextWrapping="Wrap" Width="300"
                    TextAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16"/>
            <TextBox Name="style" Text="{Binding styleProperty, Mode=OneWay}" Width="250" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <TextBlock Text="Categorie :"
                     TextWrapping="Wrap" Width="300"
                    TextAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16"/>
            <Menu HorizontalAlignment="Center" Margin="0,0,0,20">
                <MenuItem Header="Categorie disponibles :" >
                    <ListView Name="listeCategories">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Type" Width="120" DisplayMemberBinding="{Binding nomCategorieProperty}" />
                            </GridView>
                        </ListView.View>
                    </ListView>
                </MenuItem>
            </Menu>
            
                <Button x:Name="nomProduitButton" Content="Ajouter" BorderBrush="#FF545454" HorizontalAlignment="center" Height="20" Click="nomProduitButton_Click" />
                <DataGrid x:Name="listeProduits" Visibility="Hidden" ItemsSource="{Binding}" AutoGenerateColumns="false" HorizontalAlignment="left" BorderBrush="Black" Background="#FFA4A4A4" SelectionChanged="listeProduit_SelectionChanged"
                                          Height="150"
                                          ScrollViewer.CanContentScroll="True" 
                                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                                          ScrollViewer.HorizontalScrollBarVisibility="Auto" Margin="332,2,0,0" Width="107">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="id" Binding="{Binding idProperty}" />
                        <DataGridTextColumn Header="estimation" Binding="{Binding estimationProperty}" />
                        <DataGridTextColumn Header="prix de vente" Binding="{Binding prixVenteProperty}" />
                        <DataGridTextColumn Header="nom" Binding="{Binding nomProduitProperty}" />
                        <DataGridTextColumn Header="description" Binding="{Binding descriptionProperty}" />
                        <DataGridTextColumn Header="artiste" Binding="{Binding artisteProperty}" />
                        <DataGridTextColumn Header="style" Binding="{Binding styleProperty}" />
                        <DataGridTextColumn Header="categorie" />
                    </DataGrid.Columns>
                </DataGrid>
            
        </StackPanel>
    </Grid>
</UserControl>
